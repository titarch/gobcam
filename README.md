# Gobcam

A goblin in your webcam. Adds animated emoji reactions (and eventually effects) to a Linux webcam feed via v4l2loopback, so any video call app can use the modified feed as a camera source. Built because Teams won't let you thumbs-down the all-hands.
