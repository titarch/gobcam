{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the panel needs at runtime.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
