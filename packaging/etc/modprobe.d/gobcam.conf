# Gobcam: allocate one loopback at /dev/video10 with exclusive_caps so
# Chromium-based apps (Teams, Meet) discover it as a webcam. Installed
# by the gobcam .deb package and by scripts/gobcam-setup.
options v4l2loopback devices=1 video_nr=10 card_label=Gobcam exclusive_caps=1
